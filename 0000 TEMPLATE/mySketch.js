function setup() {
    createCanvas(700, 700);
	
}

function draw() {
    background(0); // black background
    translate(width / 2, height / 2); // sets origin to center of canvas

    stroke(255);
    noFill();
    strokeWeight(1);
    
    
}